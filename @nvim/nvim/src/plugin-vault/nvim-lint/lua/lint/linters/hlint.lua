local a={error=vim.diagnostic.severity.ERROR,warning=vim.diagnostic.severity.WARN,suggestion=vim.diagnostic.severity.HINT}return{cmd="hlint",args={"--json","--no-exit-code"},parser=function(b)local c={}local d=#b>0 and vim.json.decode(b)or{}for e,f in ipairs(d)do table.insert(c,{lnum=f.startLine,col=f.startColumn,end_lnum=f.endLine,end_col=f.endColumn,severity=a[f.severity:lower()],source="hlint",message=f.hint..(f.to~=vim.NIL and": "..f.to or"")})end;return c end}
