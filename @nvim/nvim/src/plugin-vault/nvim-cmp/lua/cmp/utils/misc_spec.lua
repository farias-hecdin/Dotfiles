local a=require('cmp.utils.spec')local b=require('cmp.utils.misc')describe('misc',function()before_each(a.before)it('merge',function()local c;c=b.merge({a={}},{a={b=1}})assert.are.equal(c.a.b,1)c=b.merge({a={i=1}},{a={c=2}})assert.are.equal(c.a.i,1)assert.are.equal(c.a.c,2)c=b.merge({a=false},{a={b=1}})assert.are.equal(c.a,false)c=b.merge({a=b.none},{a={b=1}})assert.are.equal(c.a,nil)c=b.merge({a=b.none},{a=nil})assert.are.equal(c.a,nil)c=b.merge({a=nil},{a=b.none})assert.are.equal(c.a,nil)end)end)
