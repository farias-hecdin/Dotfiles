local a={warning=vim.diagnostic.severity.WARN,error=vim.diagnostic.severity.ERROR}return require('lint.util').inject_cmd_exe({cmd=function()local b=vim.fn.fnamemodify("./node_modules/.bin/stylelint",":p")local c=vim.loop.fs_stat(b)if c then return b end;return"stylelint"end,stdin=true,args={"-f","json","--stdin","--stdin-filename",function()return vim.fn.expand("%:p")end},stream="stdout",ignore_exitcode=true,parser=function(d)local e,f=pcall(vim.json.decode,d)if e then f=f[1]else f={warnings={{line=1,column=1,text="Stylelint error, run `stylelint "..vim.fn.expand("%").."` for more info.",severity="error",rule="none"}},errored=true}end;local g={}if f.errored then for h,i in ipairs(f.warnings)do table.insert(g,{lnum=i.line-1,col=i.column-1,end_lnum=i.line-1,end_col=i.column-1,message=i.text,code=i.rule,user_data={lsp={code=i.rule}},severity=a[i.severity],source="stylelint"})end end;return g end})
