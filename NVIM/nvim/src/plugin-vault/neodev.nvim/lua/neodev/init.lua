local a={}local function b(c)pcall(function()require("neoconf.plugins").register({on_schema=function(d)d:import("neodev",c.defaults)d:set("neodev.library.plugins",{description="true/false or an array of plugin names to enable",anyOf={{type="boolean"},{type="array",items={type="string"}}}})end})end)end;function a.setup(e)local c=require("neodev.config")c.setup(e)if c.options.lspconfig then require("neodev.lsp").setup()end;b(c)return{settings={legacy=true}}end;return a
